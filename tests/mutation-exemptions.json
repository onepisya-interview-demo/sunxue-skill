[
  {
    "category": "tables_duplicate_in_regression_output",
    "pattern": "src/sunxue_gates/regression_output.py:_WORDS inside count_server_polyphony",
    "count": 20,
    "reason": "Local _WORDS tuple inside count_server_polyphony duplicates tables.SERVER_POLYPHONY_WORDS for the co_consts golden contract; tests only exercise one role word so 20 of 21 word-literal mutations are not killed. Canonical home is tables.SERVER_POLYPHONY_WORDS; duplication intentional (see source comment lines 129-139)."
  },
  {
    "category": "mutation_drill_run_internal",
    "pattern": "src/sunxue_gates/mutation_drill.py:run + M3 mutator functions (mutate_synonym / mutate_split / mutate_soften / extract_key_sentences)",
    "count": 130,
    "reason": "mutation_drill.run orchestrator + the four M3 mutator functions. test_mutation_drill exercises the full pipeline but does not pin per-mutator control flow branches; mutmut hits string-template assembly mutations inside the dispatch logic."
  },
  {
    "category": "lint_claims_internal_helpers",
    "pattern": "src/sunxue_gates/lint_claims.py:_check_thresholds + _check_uv_commands + _check_skill_freeze + _check_version_match + _check_directory_counts + _load_pyproject + _run",
    "count": 395,
    "reason": "Internal helpers of the W2 7th gate (claims-lint). TestLiveRepoClaims exercises happy-path assertions against the real repo root; failure-mode tests use tmp_path which mutmut's path-resolution can't map to the live repo. Coverage gap, not signal — every helper has at least one behavioural test."
  },
  {
    "category": "token_budget_run_helpers",
    "pattern": "src/sunxue_gates/token_budget.py:run + count",
    "count": 148,
    "reason": "Token-budget gate internals (heuristic estimator + tiktoken optional path). Token counting is exercised indirectly via integration but no direct mutation-test surface; small-string mutations in the heuristic word-list survive because tests only assert count bounds."
  },
  {
    "category": "lint_structure_check_one_helpers",
    "pattern": "src/sunxue_gates/lint_structure.py:_check_one + _run",
    "count": 121,
    "reason": "_check_one is the per-file structural validator; tests assert full-file PASS/FAIL but not per-line mutation sensitivity. _run aggregates per-file checks into GateResult."
  },
  {
    "category": "main_cli_dispatch",
    "pattern": "src/sunxue_gates/__main__.py:main",
    "count": 70,
    "reason": "CLI dispatch + argparse handling. test_init_and_main covers PASS/FAIL of the full gate chain but does not pin every CLI branch (--all, --json, error paths)."
  },
  {
    "category": "injection_drill_run",
    "pattern": "src/sunxue_gates/injection_drill.py:run",
    "count": 67,
    "reason": "D1-D5 drill runner; keyword matching logic tested but small literal mutations in DRILLS.keywords (which live in tables.py and are excluded) don't appear here. The runner's control-flow mutations (vector formatting, per-drill score accumulation) survive."
  },
  {
    "category": "regression_output_runner_helpers",
    "pattern": "src/sunxue_gates/regression_output.py:_check_text + run + sample_files + cmp + count_loop_closure + count_punct",
    "count": 92,
    "reason": "Gate-runner wrapper logic + sample-files registration + cmp dispatch. sample_files now contains writing-十二个字节 (W3 add); the runner's loop and reporting are tested but branch coverage is partial — tests assert on aggregate PASS/FAIL, not per-metric dispatch order."
  },
  {
    "category": "scan_security_internal_helpers",
    "pattern": "src/sunxue_gates/scan_security.py:_scan_text + scan_file + _run + collect_scan_files",
    "count": 47,
    "reason": "Pattern scanning internals. The PII/SECRET/INJECTION regex tables live in tables.py (excluded); what survives here are mutations to the scan_file dispatch + hit-truncation logic (m.group()[:40]) + collect_scan_files path glob."
  },
  {
    "category": "parsing_frontmatter",
    "pattern": "src/sunxue_gates/parsing.py:parse_frontmatter",
    "count": 7,
    "reason": "YAML frontmatter parser edge cases (whitespace handling, missing delimiters, body-separator detection). test_parsing covers happy path + one error case; corner cases uncovered."
  }
]
