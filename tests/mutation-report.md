# Mutation Triage Report

> Generated by `scripts/gen_mutation_report.py` (plan 2.1). Tool: `mutmut, version 3.7.0` against `src/sunxue_gates` (tables.py is excluded via `[tool.mutmut] do_not_mutate`). Do not hand-edit the count rows below — re-run the generator.
>
> **v1.2.2 release 描述纠错**：此 report 之前 Survivor rate 算法有 bug（分母错：1327 未杀死而非 2581 全集）。v1.2.2 audit 时记的 7.9% / 命中 M-14 < 10% 目标是误读 mutmut run UI 数字（🫥 205 是狭义 confirmed-survived，不含 🙁 1124 suspicious）。本节下方 Final tally 表格已手工修正为正确算法：`mutmut results --all` 拿全集 2581 = 1254 killed + 1327 未杀死（1122 survived + 205 no_tests）；幸存率 51.4% 与 v1.2.1 49.5% 同量级，**未命中 M-14 < 10% 目标**（属 v1.3 性能优化 backlog）。reporter 算法 bug 修在 v1.3。

Final tally (v1.2.2 manually-corrected; reporter algorithm fixed in v1.3):

| Status | Count |
|--------|-------|
| Killed by tests | 1254 |
| No tests collected | 205 |
| Survived (incl. suspicious 🙁) | 1122 |
| Timeout | 0 |
| **Total mutants** | **2581** |

Survivor rate: **51.4%** (1327/2581) — survived + no_tests = 1122 + 205 = 1327 not killed out of 2581 total.

Categorised exemption total: **1097** (spread across 10 entries — see `tests/mutation-exemptions.json`).

> **算法说明**：mutmut 3.7.0 CLI 区分 "killed"（1254，测试杀死）vs "not killed"（1327 = 1122 + 205）。"not killed" 内部又分 "Survived"（1122 = 🙁 suspicious 类，mutmut 标记为未确认）和 "no tests"（205 = 🫥 confirmed-survived 但无 test 触发）。mutmut run UI 顶部状态条用 🙁 = suspicious，🫥 = survived，本机视化标签与 CLI 标签互换。reporter 当前用 `mutmut results`（无 --all）只读未杀死部分 1327 项，误把 1122 当 "全部未杀死" 算比例得 84.6%（1122/1327），分母错。正确幸存率 = 未杀死 / 全集 = 1327/2581 = 51.4%。v1.2.1 49.5%（1098/2216）算法口径同 v1.2.2 51.4%，两个版本幸存率同量级。

---

## Exemption categories

| Category | Pattern | Mutants | Reason |
|----------|---------|---------|--------|
| tables_duplicate_in_regression_output | `src/sunxue_gates/regression_output.py:_WORDS inside count_server_polyphony` | 20 | Local _WORDS tuple inside count_server_polyphony duplicates tables.SERVER_POLYPHONY_WORDS for the co_consts golden contract; tests only exercise one role word so 20 of 21 word-literal mutations are not killed. Canonical home is tables.SERVER_POLYPHONY_WORDS; duplication intentional (see source comment lines 129-139). |
| mutation_drill_run_internal | `src/sunxue_gates/mutation_drill.py:run + M3 mutator functions (mutate_synonym / mutate_split / mutate_soften / extract_key_sentences)` | 130 | mutation_drill.run orchestrator + the four M3 mutator functions. test_mutation_drill exercises the full pipeline but does not pin per-mutator control flow branches; mutmut hits string-template assembly mutations inside the dispatch logic. |
| lint_claims_internal_helpers | `src/sunxue_gates/lint_claims.py:_check_thresholds + _check_uv_commands + _check_skill_freeze + _check_version_match + _check_directory_counts + _load_pyproject + _run` | 395 | Internal helpers of the W2 7th gate (claims-lint). TestLiveRepoClaims exercises happy-path assertions against the real repo root; failure-mode tests use tmp_path which mutmut's path-resolution can't map to the live repo. Coverage gap, not signal — every helper has at least one behavioural test. |
| token_budget_run_helpers | `src/sunxue_gates/token_budget.py:run + count` | 148 | Token-budget gate internals (heuristic estimator + tiktoken optional path). Token counting is exercised indirectly via integration but no direct mutation-test surface; small-string mutations in the heuristic word-list survive because tests only assert count bounds. |
| lint_structure_check_one_helpers | `src/sunxue_gates/lint_structure.py:_check_one + _run` | 121 | _check_one is the per-file structural validator; tests assert full-file PASS/FAIL but not per-line mutation sensitivity. _run aggregates per-file checks into GateResult. |
| main_cli_dispatch | `src/sunxue_gates/__main__.py:main` | 70 | CLI dispatch + argparse handling. test_init_and_main covers PASS/FAIL of the full gate chain but does not pin every CLI branch (--all, --json, error paths). |
| injection_drill_run | `src/sunxue_gates/injection_drill.py:run` | 67 | D1-D5 drill runner; keyword matching logic tested but small literal mutations in DRILLS.keywords (which live in tables.py and are excluded) don't appear here. The runner's control-flow mutations (vector formatting, per-drill score accumulation) survive. |
| regression_output_runner_helpers | `src/sunxue_gates/regression_output.py:_check_text + run + sample_files + cmp + count_loop_closure + count_punct` | 92 | Gate-runner wrapper logic + sample-files registration + cmp dispatch. sample_files now contains writing-十二个字节 (W3 add); the runner's loop and reporting are tested but branch coverage is partial — tests assert on aggregate PASS/FAIL, not per-metric dispatch order. |
| scan_security_internal_helpers | `src/sunxue_gates/scan_security.py:_scan_text + scan_file + _run + collect_scan_files` | 47 | Pattern scanning internals. The PII/SECRET/INJECTION regex tables live in tables.py (excluded); what survives here are mutations to the scan_file dispatch + hit-truncation logic (m.group()[:40]) + collect_scan_files path glob. |
| parsing_frontmatter | `src/sunxue_gates/parsing.py:parse_frontmatter` | 7 | YAML frontmatter parser edge cases (whitespace handling, missing delimiters, body-separator detection). test_parsing covers happy path + one error case; corner cases uncovered. |

---

## Methodology notes

- This report is rendered by `scripts/gen_mutation_report.py`. Re-run after any `mutmut run` to refresh counts.
- `tests/mutation-exemptions.json` is the single source of truth for categorised survivors; the script reads it as `[category, pattern, reason, count]` entries.
- `tests/unit/test_golden_literals.py` pins the keyword/pattern tables in `src/sunxue_gates/tables.py` via SHA-256 + UTF-8 hex against `tests/golden/literals.json`; the table-quality signal moves from mutmut to the golden contract.
